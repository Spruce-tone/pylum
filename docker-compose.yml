version: "3.8"

services:
  miniconda:
    build:
        context: .
        dockerfile: Dockerfile
    volumes:
    - ".:/spruce"
    container_name: lumerical
    ports:
    - "8888:8888"
    expose:
      - "8888"
    stdin_open: true
    tty: true